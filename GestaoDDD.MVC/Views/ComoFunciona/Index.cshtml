@model IEnumerable<GestaoDDD.Application.ViewModels.ComoFuncionaViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cf_Ordem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cf_Informacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cf_Ordem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cf_Informacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cf_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cf_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cf_Id })
        </td>
    </tr>
}

</table>
